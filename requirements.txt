# FSM pipeline runs on stdlib-only Python. No third-party deps required
# for orchestrate.py / fsm_core / audit scripts. This file exists for any
# optional tooling (e.g. pytest dev-time) and is intentionally empty.
